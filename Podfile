# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'mapbox-app' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for mapbox-app
  pod 'MapboxMaps', '~> 10.0'

  target 'mapbox-appTests' do
    inherit! :search_paths
    pod 'MapboxMaps', '~> 10.0'
    # Pods for testing
  end

  target 'mapbox-appUITests' do
    # Pods for testing
  end

end

